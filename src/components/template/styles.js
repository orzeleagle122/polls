import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
        sliderImg: {
            objectFit: "fill",
            width: "100%",
            height: "100vh",
        },
        slider: {
            height: "100vh"
        }
    }
))